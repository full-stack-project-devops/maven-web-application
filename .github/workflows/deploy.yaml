name: Update and execute EKS
on:
  push:
    branches:
      - stage
  workflow_dispatch:
    inputs:
      environment:
        type: string
        required: true
        default: staging
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
permissions:
  id-token: write
  contents: write
jobs:
  building-packing:
    name: Building and packaging maven application
    runs-on: ubuntu-latest
    environment:
      name: ${{ inputs.environment }}
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: "arn:aws:iam::605693051514:role/GITHUB_ACTIONS_ROLE"
          aws-region: "us-west-2"
      - uses: actions/checkout@v4
      - name: Set up JDK 8
        uses: actions/setup-java@v2
        with:
          java-version: '8'
          distribution: 'adopt'
          cache: maven
      - name: Build with Maven
        run: mvn -B package --file pom.xml
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2
      - name: Build, tag, and push docker image to Amazon ECR
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: maven-application
          IMAGE_TAG: ${{ github.sha }}
        run: |
          docker build -t $REGISTRY/$REPOSITORY:$GITHUB_RUN_NUMBER .
          docker build -t $REGISTRY/$REPOSITORY:stage .
          docker push $REGISTRY/$REPOSITORY:$GITHUB_RUN_NUMBER
          docker push $REGISTRY/$REPOSITORY:stage
